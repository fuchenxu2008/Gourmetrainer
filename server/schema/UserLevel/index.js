const UserType = require('./typeDef.gql');
const UserQuery = require('./query');
const UserMutation = require('./mutation');

module.exports = {
    UserType,
    UserQuery,
    UserMutation
}